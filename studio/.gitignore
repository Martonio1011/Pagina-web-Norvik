node_modules/
.next/
out/
build/
generated/

# Local database — never committed. Recreated with `npm run setup`.
*.db
*.db-journal
*.db-shm
*.db-wal
prisma/*.db*

# Secrets
.env
.env.local
.env.*.local

# Trendsi browser session (contains your login cookies)
.trendsi-session/
.cache/

# Scrape cache and generated exports
.ingest-cache/
exports/

# Test output
coverage/
playwright-report/
test-results/
*.tsbuildinfo
next-env.d.ts

# Marker written by the Windows launcher once the browser is downloaded
.playwright-ready

# Captured Trendsi responses are written here by the capture step.
# The recordings themselves are meant to be committed once reviewed;
# this ignores nothing by default and is a reminder of where they land.
