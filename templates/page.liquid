<div style="max-width:720px;margin:0 auto;padding:64px 40px 100px;">
  <h1 style="font-family:var(--font-heading);font-size:30px;letter-spacing:0.03em;color:var(--color-text);margin:0 0 32px;">{{ page.title }}</h1>
  <div style="font-family:var(--font-body);font-size:14px;color:var(--color-text-secondary);line-height:1.8;">
    {{ page.content }}
  </div>
</div>
